use = "complete.obs",
method = "pearson")
print(correlation_matrix)
# Create a vector of new names in the desired order
new_names <- c("ln(below poverty)", "ln(percent vacancies)", "ln(percent bachelor's+)", "ln(percent detached)") # Adjust to your number of variables
# Assign the new names to the dimensions of the matrix
colnames(correlation_matrix) <- new_names
rownames(correlation_matrix) <- new_names
corrplot(correlation_matrix,
method = "color", # Use "color" for the background fill
type = "upper",
order = "hclust",
col = colorRampPalette(c("red", "white", "blue"))(200),
tl.col = "black",
tl.srt = 45,
addCoef.col = "black", # Add coefficients with black text color
number.cex = 0.7 # Adjust the size of the numbers if needed
)
correlation_matrix <- cor(regress_data[, c("LNPCTBACHMORE", "LNNBELPOV100", "LNPCTVACANT", "LNPCTSINGLES")],
use = "complete.obs",
method = "pearson")
print(correlation_matrix)
# Create a vector of new names in the desired order
new_names <- c("ln(# Below Poverty)", "ln(% Vacancies)", "ln(% Bachelor's +)", "ln(% Detached)") # Adjust to your number of variables
# Assign the new names to the dimensions of the matrix
colnames(correlation_matrix) <- new_names
rownames(correlation_matrix) <- new_names
corrplot(correlation_matrix,
method = "color", # Use "color" for the background fill
type = "upper",
order = "hclust",
col = colorRampPalette(c("red", "white", "blue"))(200),
tl.col = "black",
tl.srt = 45,
addCoef.col = "black", # Add coefficients with black text color
number.cex = 0.7 # Adjust the size of the numbers if needed
)
correlation_matrix <- cor(regress_data[, c("LNPCTBACHMORE", "LNNBELPOV100", "LNPCTVACANT", "LNPCTSINGLES")],
use = "complete.obs",
method = "pearson")
print(correlation_matrix)
# Create a vector of new names in the desired order
#new_names <- c("ln(# Below Poverty)", "ln(% Vacancies)", "ln(% Bachelor's +)", "ln(% Detached)") # Adjust to your number of variables
# Assign the new names to the dimensions of the matrix
#colnames(correlation_matrix) <- new_names
#rownames(correlation_matrix) <- new_names
corrplot(correlation_matrix,
method = "color", # Use "color" for the background fill
type = "upper",
order = "hclust",
col = colorRampPalette(c("red", "white", "blue"))(200),
tl.col = "black",
tl.srt = 45,
addCoef.col = "black", # Add coefficients with black text color
number.cex = 0.7 # Adjust the size of the numbers if needed
)
correlation_matrix <- cor(regress_data[, c("LNPCTBACHMORE", "LNNBELPOV100", "LNPCTVACANT", "LNPCTSINGLES")],
use = "complete.obs",
method = "pearson")
print(correlation_matrix)
# Create a vector of new names in the desired order
new_names <- c("ln(% Bachelor's +)", "ln(# Below Poverty)", "ln(% Vacancies)", "ln(% Detached)") # Adjust to your number of variables
# Assign the new names to the dimensions of the matrix
colnames(correlation_matrix) <- new_names
rownames(correlation_matrix) <- new_names
corrplot(correlation_matrix,
method = "color", # Use "color" for the background fill
type = "upper",
order = "hclust",
col = colorRampPalette(c("red", "white", "blue"))(200),
tl.col = "black",
tl.srt = 45,
addCoef.col = "black", # Add coefficients with black text color
number.cex = 0.7 # Adjust the size of the numbers if needed
)
correlation_matrix <- cor(regress_data[, c("LNPCTBACHMORE", "LNNBELPOV100", "LNPCTVACANT", "LNPCTSINGLES")],
use = "complete.obs",
method = "pearson")
print(correlation_matrix)
# Create a vector of new names in the desired order
#new_names <- c("ln(% Bachelor's +)", "ln(# Below Poverty)", "ln(% Vacancies)", "ln(% Detached)") # Adjust to your number of variables
# Assign the new names to the dimensions of the matrix
#colnames(correlation_matrix) <- new_names
#rownames(correlation_matrix) <- new_names
corrplot(correlation_matrix,
method = "color", # Use "color" for the background fill
type = "upper",
order = "hclust",
col = colorRampPalette(c("red", "white", "blue"))(200),
tl.col = "black",
tl.srt = 45,
addCoef.col = "black", # Add coefficients with black text color
number.cex = 0.7 # Adjust the size of the numbers if needed
)
correlation_matrix <- cor(regress_data[, c("LNPCTBACHMORE", "LNNBELPOV100", "LNPCTVACANT", "LNPCTSINGLES")],
use = "complete.obs",
method = "pearson")
print(correlation_matrix)
# Create a vector of new names in the desired order
new_names <- c("ln(% Bachelor's +)", "ln(# Below Poverty)", "ln(% Vacancies)", "ln(% Detached)") # Adjust to your number of variables
# Assign the new names to the dimensions of the matrix
colnames(correlation_matrix) <- new_names
rownames(correlation_matrix) <- new_names
corrplot(correlation_matrix,
method = "color", # Use "color" for the background fill
type = "upper",
order = "hclust",
col = colorRampPalette(c("red", "white", "blue"))(200),
tl.col = "black",
tl.srt = 45,
addCoef.col = "black", # Add coefficients with black text color
number.cex = 0.7 # Adjust the size of the numbers if needed
)
correlation_matrix <- cor(regress_data[, c("LNPCTBACHMORE", "LNNBELPOV100", "LNPCTVACANT", "LNPCTSINGLES")],
use = "complete.obs",
method = "pearson")
print(correlation_matrix)
# Create a vector of new names in the desired order
new_names <- c("ln(% Bachelor's +)", "ln(# Below Poverty)", "ln(% Vacancies)", "ln(% Detached)") # Adjust to your number of variables
# Assign the new names to the dimensions of the matrix
colnames(correlation_matrix) <- new_names
rownames(correlation_matrix) <- new_names
corrplot(correlation_matrix,
method = "color", # Use "color" for the background fill
type = "upper",
order = "hclust",
col = colorRampPalette(c("red", "white", "blue"))(200),
tl.col = "black",
tl.srt = 45,
mar = c(0, 0, 1, 0), # Set plot margins (bottom, left, top, right)
addCoef.col = "black", # Add coefficients with black text color
number.cex = 0.7 # Adjust the size of the numbers if needed
)
correlation_matrix <- cor(regress_data[, c("LNPCTBACHMORE", "LNNBELPOV100", "LNPCTVACANT", "LNPCTSINGLES")],
use = "complete.obs",
method = "pearson")
print(correlation_matrix)
# Create a vector of new names in the desired order
new_names <- c("ln(% Bachelor's +)", "ln(# Below Poverty)", "ln(% Vacancies)", "ln(% Detached)") # Adjust to your number of variables
# Assign the new names to the dimensions of the matrix
colnames(correlation_matrix) <- new_names
rownames(correlation_matrix) <- new_names
corrplot(correlation_matrix,
method = "color", # Use "color" for the background fill
type = "upper",
order = "hclust",
col = colorRampPalette(c("red", "white", "blue"))(200),
tl.col = "black",
tl.srt = 45,
mar = c(0, 5, 5, 5), # Set plot margins (bottom, left, top, right)
addCoef.col = "black", # Add coefficients with black text color
number.cex = 0.7 # Adjust the size of the numbers if needed
)
correlation_matrix <- cor(regress_data[, c("LNPCTBACHMORE", "LNNBELPOV100", "LNPCTVACANT", "LNPCTSINGLES")],
use = "complete.obs",
method = "pearson")
print(correlation_matrix)
# Create a vector of new names in the desired order
new_names <- c("ln(% Bachelor's +)", "ln(# Below Poverty)", "ln(% Vacancies)", "ln(% Detached)") # Adjust to your number of variables
# Assign the new names to the dimensions of the matrix
colnames(correlation_matrix) <- new_names
rownames(correlation_matrix) <- new_names
corrplot(correlation_matrix,
method = "color", # Use "color" for the background fill
type = "upper",
order = "hclust",
col = colorRampPalette(c("red", "white", "blue"))(200),
tl.col = "black",
tl.srt = 45,
mar = c(0, -5, -5, -5), # Set plot margins (bottom, left, top, right)
addCoef.col = "black", # Add coefficients with black text color
number.cex = 0.7 # Adjust the size of the numbers if needed
)
correlation_matrix <- cor(regress_data[, c("LNPCTBACHMORE", "LNNBELPOV100", "LNPCTVACANT", "LNPCTSINGLES")],
use = "complete.obs",
method = "pearson")
print(correlation_matrix)
# Create a vector of new names in the desired order
new_names <- c("ln(% Bachelor's +)", "ln(# Below Poverty)", "ln(% Vacancies)", "ln(% Detached)") # Adjust to your number of variables
# Assign the new names to the dimensions of the matrix
colnames(correlation_matrix) <- new_names
rownames(correlation_matrix) <- new_names
corrplot(correlation_matrix,
method = "color", # Use "color" for the background fill
type = "upper",
order = "hclust",
col = colorRampPalette(c("red", "white", "blue"))(200),
tl.col = "black",
tl.srt = 45,
#mar = c(0, 5, 5, 5), # Set plot margins (bottom, left, top, right)
addCoef.col = "black", # Add coefficients with black text color
number.cex = 0.7 # Adjust the size of the numbers if needed
)
correlation_matrix <- cor(regress_data[, c("LNPCTBACHMORE", "LNNBELPOV100", "LNPCTVACANT", "LNPCTSINGLES")],
use = "complete.obs",
method = "pearson")
print(correlation_matrix)
# Create a vector of new names in the desired order
new_names <- c("ln(% Bachelor's +)", "ln(# Below Poverty)", "ln(% Vacancies)", "ln(% Detached)") # Adjust to your number of variables
# Assign the new names to the dimensions of the matrix
colnames(correlation_matrix) <- new_names
rownames(correlation_matrix) <- new_names
par(mar = c(5, 5, 2, 2)) # bottom, left, top, right
corrplot(correlation_matrix,
method = "color", # Use "color" for the background fill
type = "upper",
order = "hclust",
col = colorRampPalette(c("red", "white", "blue"))(200),
tl.col = "black",
tl.srt = 45,
addCoef.col = "black", # Add coefficients with black text color
number.cex = 0.7 # Adjust the size of the numbers if needed
)
correlation_matrix <- cor(regress_data[, c("LNPCTBACHMORE", "LNNBELPOV100", "LNPCTVACANT", "LNPCTSINGLES")],
use = "complete.obs",
method = "pearson")
print(correlation_matrix)
# Create a vector of new names in the desired order
new_names <- c("ln(% Bachelor's +)", "ln(# Below Poverty)", "ln(% Vacancies)", "ln(% Detached)") # Adjust to your number of variables
# Assign the new names to the dimensions of the matrix
colnames(correlation_matrix) <- new_names
rownames(correlation_matrix) <- new_names
corrplot(correlation_matrix,
method = "color", # Use "color" for the background fill
type = "upper",
order = "hclust",
col = colorRampPalette(c("red", "white", "blue"))(200),
tl.col = "black",
tl.srt = 45,
addCoef.col = "black", # Add coefficients with black text color
number.cex = 0.7 # Adjust the size of the numbers if needed
)
correlation_matrix <- cor(regress_data[, c("LNPCTBACHMORE", "LNNBELPOV100", "LNPCTVACANT", "LNPCTSINGLES")],
use = "complete.obs",
method = "pearson")
print(correlation_matrix)
# Create a vector of new names in the desired order
new_names <- c("ln(% Bachelor's +)", "ln(# Below Poverty)", "ln(% Vacancies)", "ln(% Detached)") # Adjust to your number of variables
# Assign the new names to the dimensions of the matrix
colnames(correlation_matrix) <- new_names
rownames(correlation_matrix) <- new_names
ggcorrplot(correlation_matrix, hc.order = TRUE, type = "lower",
outline.col = "white",
ggtheme = ggplot2::theme_gray,
colors = c("#6D9EC1", "white", "#E46726"))
library(ggcorrplot)
install.packages("ggcorrplot")
library(ggcorrplot)
correlation_matrix <- cor(regress_data[, c("LNPCTBACHMORE", "LNNBELPOV100", "LNPCTVACANT", "LNPCTSINGLES")],
use = "complete.obs",
method = "pearson")
print(correlation_matrix)
# Create a vector of new names in the desired order
new_names <- c("ln(% Bachelor's +)", "ln(# Below Poverty)", "ln(% Vacancies)", "ln(% Detached)") # Adjust to your number of variables
# Assign the new names to the dimensions of the matrix
colnames(correlation_matrix) <- new_names
rownames(correlation_matrix) <- new_names
ggcorrplot(correlation_matrix, hc.order = TRUE, type = "lower",
outline.col = "white",
ggtheme = ggplot2::theme_gray,
colors = c("#6D9EC1", "white", "#E46726"))
corrplot(correlation_matrix,
method = "color", # Use "color" for the background fill
type = "upper",
order = "hclust",
col = colorRampPalette(c("red", "white", "blue"))(200),
tl.col = "black",
tl.srt = 45,
addCoef.col = "black", # Add coefficients with black text color
number.cex = 0.7 # Adjust the size of the numbers if needed
)
correlation_matrix <- cor(regress_data[, c("LNPCTBACHMORE", "LNNBELPOV100", "LNPCTVACANT", "LNPCTSINGLES")],
use = "complete.obs",
method = "pearson")
print(correlation_matrix)
# Create a vector of new names in the desired order
new_names <- c("ln(% Bachelor's +)", "ln(# Below Poverty)", "ln(% Vacancies)", "ln(% Detached)") # Adjust to your number of variables
# Assign the new names to the dimensions of the matrix
colnames(correlation_matrix) <- new_names
rownames(correlation_matrix) <- new_names
ggcorrplot(correlation_matrix, type = "lower",
outline.col = "white",
ggtheme = ggplot2::theme_gray,
colors = c("#6D9EC1", "white", "#E46726"))
corrplot(correlation_matrix,
method = "color", # Use "color" for the background fill
type = "upper",
order = "hclust",
col = colorRampPalette(c("red", "white", "blue"))(200),
tl.col = "black",
tl.srt = 45,
addCoef.col = "black", # Add coefficients with black text color
number.cex = 0.7 # Adjust the size of the numbers if needed
)
correlation_matrix <- cor(regress_data[, c("LNPCTBACHMORE", "LNNBELPOV100", "LNPCTVACANT", "LNPCTSINGLES")],
use = "complete.obs",
method = "pearson")
print(correlation_matrix)
# Create a vector of new names in the desired order
new_names <- c("ln(% Bachelor's +)", "ln(# Below Poverty)", "ln(% Vacancies)", "ln(% Detached)") # Adjust to your number of variables
# Assign the new names to the dimensions of the matrix
colnames(correlation_matrix) <- new_names
rownames(correlation_matrix) <- new_names
ggcorrplot(correlation_matrix, hc.order = TRUE, use = "everything", type = "lower",
outline.col = "white",
ggtheme = ggplot2::theme_gray,
colors = c("#6D9EC1", "white", "#E46726"))
correlation_matrix <- cor(regress_data[, c("LNPCTBACHMORE", "LNNBELPOV100", "LNPCTVACANT", "LNPCTSINGLES")],
use = "everything",
method = "pearson")
print(correlation_matrix)
# Create a vector of new names in the desired order
new_names <- c("ln(% Bachelor's +)", "ln(# Below Poverty)", "ln(% Vacancies)", "ln(% Detached)") # Adjust to your number of variables
# Assign the new names to the dimensions of the matrix
colnames(correlation_matrix) <- new_names
rownames(correlation_matrix) <- new_names
ggcorrplot(correlation_matrix, hc.order = TRUE, type = "lower",
outline.col = "white",
ggtheme = ggplot2::theme_gray,
colors = c("#6D9EC1", "white", "#E46726"))
corrplot(correlation_matrix,
method = "color", # Use "color" for the background fill
type = "upper",
order = "hclust",
col = colorRampPalette(c("red", "white", "blue"))(200),
tl.col = "black",
tl.srt = 45,
addCoef.col = "black", # Add coefficients with black text color
number.cex = 0.7 # Adjust the size of the numbers if needed
)
correlation_matrix <- cor(regress_data[, c("LNPCTBACHMORE", "LNNBELPOV100", "LNPCTVACANT", "LNPCTSINGLES")],
use = "complete.obs",
method = "pearson")
print(correlation_matrix)
# Create a vector of new names in the desired order
new_names <- c("ln(% Bachelor's +)", "ln(# Below Poverty)", "ln(% Vacancies)", "ln(% Detached)") # Adjust to your number of variables
# Assign the new names to the dimensions of the matrix
colnames(correlation_matrix) <- new_names
rownames(correlation_matrix) <- new_names
ggcorrplot(correlation_matrix, hc.order = TRUE,
outline.col = "white",
ggtheme = ggplot2::theme_gray,
colors = c("#6D9EC1", "white", "#E46726"))
corrplot(correlation_matrix,
method = "color", # Use "color" for the background fill
type = "upper",
order = "hclust",
col = colorRampPalette(c("red", "white", "blue"))(200),
tl.col = "black",
tl.srt = 45,
addCoef.col = "black", # Add coefficients with black text color
number.cex = 0.7 # Adjust the size of the numbers if needed
)
correlation_matrix <- cor(regress_data[, c("LNPCTBACHMORE", "LNNBELPOV100", "LNPCTVACANT", "LNPCTSINGLES")],
use = "complete.obs",
method = "pearson")
print(correlation_matrix)
# Create a vector of new names in the desired order
new_names <- c("ln(% Bachelor's +)", "ln(# Below Poverty)", "ln(% Vacancies)", "ln(% Detached)") # Adjust to your number of variables
# Assign the new names to the dimensions of the matrix
colnames(correlation_matrix) <- new_names
rownames(correlation_matrix) <- new_names
ggcorrplot(correlation_matrix, hc.order = TRUE,
outline.col = "white",
ggtheme = ggplot2::theme_gray,
colors = c("#6D9EC1", "white", "#E46726"))
correlation_matrix <- cor(regress_data[, c("LNPCTBACHMORE", "LNNBELPOV100", "LNPCTVACANT", "LNPCTSINGLES")],
use = "complete.obs",
method = "pearson")
print(correlation_matrix)
# Create a vector of new names in the desired order
new_names <- c("ln(% Bachelor's +)", "ln(# Below Poverty)", "ln(% Vacancies)", "ln(% Detached)") # Adjust to your number of variables
# Assign the new names to the dimensions of the matrix
colnames(correlation_matrix) <- new_names
rownames(correlation_matrix) <- new_names
ggcorrplot(correlation_matrix,
hc.order = TRUE,
outline.col = "white",
ggtheme = ggplot2::theme_gray,
colors = c("#6D9EC1", "white", "#E46726"),
lab = TRUE,
lab_size = 3, # Adjust label size
digits = 2) # Display 2 decimal places
correlation_matrix <- cor(regress_data[, c("LNPCTBACHMORE", "LNNBELPOV100", "LNPCTVACANT", "LNPCTSINGLES")],
use = "complete.obs",
method = "pearson")
print(correlation_matrix)
# Create a vector of new names in the desired order
new_names <- c("ln(% Bachelor's +)", "ln(# Below Poverty)", "ln(% Vacancies)", "ln(% Detached)") # Adjust to your number of variables
# Assign the new names to the dimensions of the matrix
colnames(correlation_matrix) <- new_names
rownames(correlation_matrix) <- new_names
ggcorrplot(correlation_matrix,
hc.order = TRUE,
outline.col = "white",
ggtheme = ggplot2::theme_gray,
colors = c("#6D9EC1", "white", "#E46726"),
lab = TRUE,
lab_size = 3, # Adjust label size
digits = 2,
diag = TRUE) # Display 2 decimal places
correlation_matrix <- cor(regress_data[, c("LNPCTBACHMORE", "LNNBELPOV100", "LNPCTVACANT", "LNPCTSINGLES")],
use = "complete.obs",
method = "pearson")
print(correlation_matrix)
# Create a vector of new names in the desired order
new_names <- c("ln(% Bachelor's +)", "ln(# Below Poverty)", "ln(% Vacancies)", "ln(% Detached)") # Adjust to your number of variables
# Assign the new names to the dimensions of the matrix
colnames(correlation_matrix) <- new_names
rownames(correlation_matrix) <- new_names
ggcorrplot(correlation_matrix,
hc.order = TRUE,
outline.col = "white",
ggtheme = ggplot2::theme_gray,
colors = c("#6D9EC1", "white", "#E46726"),
lab = TRUE,
lab_size = 3, # Adjust label size
digits = 2,
type = "full",
diag = TRUE) # Display 2 decimal places
correlation_matrix <- cor(regress_data[, c("LNPCTBACHMORE", "LNNBELPOV100", "LNPCTVACANT", "LNPCTSINGLES")],
use = "complete.obs",
method = "pearson")
print(correlation_matrix)
# Create a vector of new names in the desired order
new_names <- c("ln(% Bachelor's +)", "ln(# Below Poverty)", "ln(% Vacancies)", "ln(% Detached)") # Adjust to your number of variables
# Assign the new names to the dimensions of the matrix
colnames(correlation_matrix) <- new_names
rownames(correlation_matrix) <- new_names
ggcorrplot(correlation_matrix,
hc.order = TRUE,
outline.col = "white",
ggtheme = ggplot2::theme_gray,
colors = c("#6D9EC1", "white", "#E46726"),
lab = TRUE,
lab_size = 3, # Adjust label size
digits = 2,
type = "full",
diag = TRUE) # Display 2 decimal places
correlation_matrix <- cor(regress_data[, c("LNPCTBACHMORE", "LNNBELPOV100", "LNPCTVACANT", "LNPCTSINGLES")],
use = "complete.obs",
method = "pearson")
print(correlation_matrix)
# Create a vector of new names in the desired order
new_names <- c("ln(% Bachelor's +)", "ln(# Below Poverty)", "ln(% Vacancies)", "ln(% Detached)") # Adjust to your number of variables
# Assign the new names to the dimensions of the matrix
colnames(correlation_matrix) <- new_names
rownames(correlation_matrix) <- new_names
ggcorrplot(correlation_matrix,
hc.order = TRUE,
outline.col = "white",
ggtheme = ggplot2::theme_gray,
colors = c("#6D9EC1", "white", "#E46726"),
lab = TRUE,
lab_size = 3, # Adjust label size
digits = 2,
type = "upper",
diag = TRUE) # Display 2 decimal places
correlation_matrix <- cor(regress_data[, c("LNPCTBACHMORE", "LNNBELPOV100", "LNPCTVACANT", "LNPCTSINGLES")],
use = "complete.obs",
method = "pearson")
print(correlation_matrix)
# Create a vector of new names in the desired order
new_names <- c("ln(% Bachelor's +)", "ln(# Below Poverty)", "ln(% Vacancies)", "ln(% Detached)") # Adjust to your number of variables
# Assign the new names to the dimensions of the matrix
colnames(correlation_matrix) <- new_names
rownames(correlation_matrix) <- new_names
ggcorrplot(correlation_matrix,
hc.order = TRUE,
outline.col = "white",
ggtheme = ggplot2::theme_gray,
colors = c("#6D9EC1", "white", "#E46726"),
lab = TRUE,
lab_size = 3, # Adjust label size
digits = 2,
type = "full") # Display 2 decimal places
correlation_matrix <- cor(regress_data[, c("LNPCTBACHMORE", "LNNBELPOV100", "LNPCTVACANT", "LNPCTSINGLES")],
use = "complete.obs",
method = "pearson")
print(correlation_matrix)
# Create a vector of new names in the desired order
new_names <- c("ln(% Bachelor's +)", "ln(# Below Poverty)", "ln(% Vacancies)", "ln(% Detached)") # Adjust to your number of variables
# Assign the new names to the dimensions of the matrix
colnames(correlation_matrix) <- new_names
rownames(correlation_matrix) <- new_names
ggcorrplot(correlation_matrix,
hc.order = TRUE,
outline.col = "white",
ggtheme = ggplot2::theme_gray,
colors = c("#6D9EC1", "white", "#E46726"),
lab = TRUE,
lab_size = 3, # Adjust label size
digits = 2,
type = "upper") # Display 2 decimal places
correlation_matrix <- cor(regress_data[, c("LNPCTBACHMORE", "LNNBELPOV100", "LNPCTVACANT", "LNPCTSINGLES")],
use = "complete.obs",
method = "pearson")
print(correlation_matrix)
# Create a vector of new names in the desired order
new_names <- c("ln(% Bachelor's +)", "ln(# Below Poverty)", "ln(% Vacancies)", "ln(% Detached)") # Adjust to your number of variables
# Assign the new names to the dimensions of the matrix
colnames(correlation_matrix) <- new_names
rownames(correlation_matrix) <- new_names
ggcorrplot(correlation_matrix,
hc.order = TRUE,
outline.col = "white",
ggtheme = ggplot2::theme_gray,
colors = c("#6D9EC1", "white", "#E46726"),
lab = TRUE,
lab_size = 3, # Adjust label size
digits = 2,
type = "full") # Display 2 decimal places
